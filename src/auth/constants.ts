export const jwtConstants = {
    secret: 'yourSecretKey', // Replace with your actual secret key
};
// Ensure to keep this secret key secure and not hard-coded in production code.
// Consider using environment variables or a secure vault for sensitive information.